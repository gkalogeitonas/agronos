version: "3.8"

services:
  # publish DB port so host processes (php artisan, etc.) can connect
  db:
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1"]
      interval: 5s
      timeout: 2s
      retries: 12

